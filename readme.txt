Pour démarrer le projet il faut lancer les commandes suivantes:

Pour ceci vous devez installer Node sur votre machine:
https://nodejs.org/en/

Ensuite il faut lancer les commandes suivantes à la racine du projet:
npm install
npm start

Le projet sera ensuite visible sur localhost:3000 sur votre navigateur
